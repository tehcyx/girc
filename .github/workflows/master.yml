name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: run make, then build docker
      run:  |
        make docker
        docker build -t girc .
