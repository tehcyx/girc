name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: run make
      run: make
    - name: Run a multi-line script
      run: |
        docker build -t girc .
